import {nav,footer,button} from './ui.mjs';
export function specimen(){return `${nav('../')}<main id="main" class="wrap specimen"><p class="eyebrow">Nicolas Sanabria / Portfolio v2</p><h1>THE DESIGN<br>LANGUAGE.</h1><p class="specimen-intro">A working specimen of the typography, tokens, and primitives used throughout the portfolio.</p><section><p class="eyebrow">01 / Palette</p><div class="swatches">${[['Acid','--yellow','#edff50'],['Ink','--black','#10110f'],['Paper','--cream','#f5f4ee'],['White','--white','#ffffff']].map(([name,token,color])=>`<div><div style="background:var(${token})"></div><strong>${name}</strong><span class="detail">${color}</span></div>`).join('')}</div></section><section><p class="eyebrow">02 / Typography</p><div class="type-specimen"><span class="detail muted">Anton / Display</span><h2>PEOPLE FIRST.<br>PIXELS SECOND.</h2></div><div class="type-specimen"><span class="detail muted">Helvetica Neue → Helvetica → Arial / Reading</span><p class="summary-lead">Good design makes the next step feel clear.</p><p class="prose">I turn complex workflows into useful experiences. The reading face gives case studies a comfortable rhythm, with room for the evidence and the decisions behind the work.</p></div><div class="type-specimen"><span class="detail muted">IBM Plex Mono / Detail</span><p class="eyebrow">01 / Research, interaction, and systems</p><p class="detail">Nicolas Sanabria · Product designer · NYC</p></div></section><section><p class="eyebrow">03 / Primitives</p><div class="specimen-actions">${button('Explore selected work','../#portfolio',true)}${button('About Nicolas','../About/')}<a class="text-link" href="../fohlio/">Read the case study ↗</a></div></section><section><p class="eyebrow">04 / Spacing</p><div class="spacing-specimen">${[1,2,3,4,6,8,12,16,24,32].map(n=>`<div><span class="detail">${n*4}px</span><i style="width:var(--space-${n})"></i></div>`).join('')}</div></section><section data-theme="dark" class="specimen-dark"><p class="eyebrow">05 / Semantic surfaces</p><h2>THE WORK<br>TAKES THE STAGE.</h2><p class="muted">The same text, surface, border, and focus roles adapt to the dark project collection.</p>${button('See the work','../#portfolio')}</section></main>${footer('../')}`;}
